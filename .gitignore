# PDFs (reports/handbooks — keep out of git)
*.pdf
*.PDF

# OS
.DS_Store

# Local Claude config (per-machine)
.claude/

# Build artifacts
build/
*.o
*.so

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# Local lerobot checkout (huggingface/lerobot, pinned via LEROBOT_VENV_SETUP.md;
# not a submodule in this repo). Large; never commit.
/lerobot/

# Data / models / logs (large artifacts)
# NOTE: ignore dataset dirs by specific name, NOT a bare `data/` — that would
# also drop the tracked `training/src/data/` package.
*.ckpt
*.pt
*.pth
*.safetensors
wandb/
outputs/
checkpoints/
datasets/
raw_datasets/
cleaned_datasets/
lerobot_datasets/
raw_recordings/
recordings/
recordings_cc/
*.mp4
*.bag

# Secrets
.env
*.local
CREDENTIALS.md

# Coverage / test artifacts
.coverage
.coverage.*
coverage.xml
htmlcov/
.pytest_cache/
*.onnx
